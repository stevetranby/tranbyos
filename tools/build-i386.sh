#!/usr/bin/env bash

# Maybe we want to handle the build more programmatically?
# Remove dependence on Makefile?
# Makefile does have a reasonable dependency system
# - `compile` depends on ``

#run build-base.sh
 
#all
#assemble
#compile
#link
